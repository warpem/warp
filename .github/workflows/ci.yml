name: CI

on:
  push:
    branches:
      - main
    tags:
      # push a tagged commit to trigger a release...
      # e.g. git tag -am v0.0.1 "v0.0.1"; git push --follow-tags
      - "v*"

# cancel in-progress runs that use the same workflow and branch
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    name: Deploy
    if: success() && startsWith(github.ref, 'refs/tags/') && github.event_name != 'schedule'
    runs-on: linux-cpu-x64-8core
    timeout-minutes: 120

    permissions:
      # This permission allows writing releases
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Micromamba
        uses: mamba-org/setup-micromamba@v2
        with:
          environment-name: package-build
          create-args: >-
            python=3.10
            conda-build
            anaconda-client
          init-shell: bash
          cache-environment: true
          post-cleanup: 'all'
          
      - name: Build conda package
        shell: micromamba-shell {0}
        env:
          CONDA_OVERRIDE_CUDA: "12.9"
        run: |
          # Work around dotnet deactivate.d/dotnet-root.sh referencing unbound $2.
          # BASH_ENV is sourced by every non-interactive bash subprocess, including
          # the ones conda-build spawns for activation/deactivation scripts.
          echo 'set +u' > /tmp/no-strict-unset.sh
          export BASH_ENV=/tmp/no-strict-unset.sh
          conda-build conda-recipe -c nvidia/label/cuda-12.9.0 -c conda-forge

      - name: Upload conda package
        shell: micromamba-shell {0}
        env:
          ANACONDA_API_TOKEN: ${{ secrets.ANACONDA_API_KEY }}
          CONDA_OVERRIDE_CUDA: "12.9"
        run: |
          anaconda upload --user warpem $(conda build --output conda-recipe -c nvidia/label/cuda-12.9.0 -c conda-forge)

      - name: Release the source on GitHub
        uses: softprops/action-gh-release@v2
